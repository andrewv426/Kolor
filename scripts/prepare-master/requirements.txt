# Python 3.10 — RAW demosaic step ONLY (scripts/prepare-master/demosaic.py).
# The rest of the pipeline (index.mjs) is Node + sharp + ffmpeg-static.
# Exact pins (no ranges) so the demosaic is byte-deterministic per CPU arch.
rawpy==0.27.0
tifffile==2025.5.10
numpy==2.2.6
